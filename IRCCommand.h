/*  * File:   IRCCommand.h * Author: Florian * * Created on 22. Februar 2015, 16:54 */#pragma once#include <string>#include "main.h"#include "IRCBot.h"class IRCCommand {public:    IRCCommand(std::string cmd, IRCBot *bot, int lvl);    IRCCommand(const IRCCommand& orig);    virtual ~IRCCommand();    void virtual CommandRecieved(std::string from, std::string in);    const std::string cmd;    int accessLvl;protected:    IRCBot *bot;};