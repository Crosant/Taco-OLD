/*  * File:   IRCChatListener.h * Author: Florian * * Created on 20. Februar 2015, 23:10 */#pragma once#include <string>#include <boost/algorithm/string.hpp>#include "main.h"class IRCChatListener {public:    IRCChatListener(const std::string chan, IRCBot *bot);    IRCChatListener(const IRCChatListener& orig);    virtual ~IRCChatListener();    void Analyze(std::string message);    std::string chan;private:    IRCBot *bot;};